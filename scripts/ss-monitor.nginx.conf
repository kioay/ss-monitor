# Place these server blocks inside the nginx `http { ... }` block on production.
# The app itself should listen on 127.0.0.1:8787 so the raw IP cannot expose it.

server {
    listen 0.0.0.0:80 default_server;
    server_name _;
    return 444;
}

server {
    listen 0.0.0.0:80;
    server_name ss-monitor.qinoay.top;

    location / {
        proxy_pass http://127.0.0.1:8787;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;
    }
}

# Optional HTTPS block for production once a certificate exists for
# ss-monitor.qinoay.top. Enable this only after the certificate/key paths are
# present, then reload nginx as root.
#
# server {
#     listen 0.0.0.0:443 ssl http2;
#     server_name ss-monitor.qinoay.top;
#
#     ssl_certificate /etc/letsencrypt/live/ss-monitor.qinoay.top/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/ss-monitor.qinoay.top/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:8787;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_read_timeout 120s;
#         proxy_send_timeout 120s;
#     }
# }
